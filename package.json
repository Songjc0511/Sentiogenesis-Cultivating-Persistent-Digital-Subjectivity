{
  "name": "electronic-life-petri-dish",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "kernel": "PYTHONPATH=python python -m sentiogenesis.server",
    "python:test": "PYTHONPATH=python python -m unittest discover -s python/tests -v",
    "build": "tsc && vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
